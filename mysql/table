USE movie;
DROP TABLE users;
CREATE TABLE users (
username VARCHAR(50) NOT NULL PRIMARY KEY,
hash CHAR(60) NOT NULL,
email VARCHAR(50),
name VARCHAR(50),
major VARCHAR(50),
interests TEXT,
isAdmin BIT,
isBanned BIT
);